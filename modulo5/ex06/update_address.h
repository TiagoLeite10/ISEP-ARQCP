/* Ficheiro de cabeçalho para o ficheiro de código update_address.s */
#ifndef UPDATE_ADDRESS_H
#define UPDATE_ADDRESS_H
	#include "student.h"
	void update_address(Student *s, char *new_address);
#endif
